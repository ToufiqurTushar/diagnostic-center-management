name: example
description: Flutter Rapid Framework all features example project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_rapid:
    git:
      url: https://github.com/banglafighter/flutter_rapid.git
      ref: dev
      path: flutter_rapid

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
